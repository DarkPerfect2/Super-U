# ==================== SERVER (Production) ====================
NODE_ENV=production
PORT=5000

# Séparation front/back en production
# L'API ne sert pas le build front, celui-ci est servi par Nginx/CDN
SERVE_CLIENT=false
# API_ONLY n'est pas requis en prod (le serveur n'intègre pas Vite en NODE_ENV=production)
# API_ONLY=false

# ==================== ORIGINES / CORS ====================
# Remplacez par votre domaine front (ex: https://shop.mondomaine.com)
FRONTEND_URL=https://shop.example.com
# Facultatif: liste CSV si plusieurs frontends
# CORS_ALLOWED_ORIGINS=https://shop.example.com,https://admin.example.com

# Cookies cross-site (si vous en servez):
# Mettre le domaine racine pour partager entre sous-domaines (ex: .example.com)
COOKIE_DOMAIN=.example.com

# ==================== JWT ====================
# Choisissez une valeur forte et secrète, identique entre déploiements
JWT_SECRET=change-me-please

# ==================== DATABASE (MongoDB Atlas) ====================
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=giantcasino

# ==================== EMAIL (Gmail SMTP) ====================
GMAIL_USER=votre-email@gmail.com
GMAIL_APP_PASSWORD=votre-mot-de-passe-application

# ==================== SMS (SMS.to) ====================
SMS_TO_API_KEY=votre-cle-api-sms-to

# ==================== CLOUDINARY ====================
CLOUDINARY_CLOUD_NAME=votre-cloud-name
CLOUDINARY_API_KEY=votre-api-key
CLOUDINARY_API_SECRET=votre-api-secret
